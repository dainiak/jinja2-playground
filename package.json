{
	"name": "jinja2-playground",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"test": "playwright test",
		"test:unit": "vitest run",
		"test:ui": "playwright test --ui",
		"lint": "biome check .",
		"format": "biome format --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.10",
		"@playwright/test": "^1.50.0",
		"vite": "^8.0.5",
		"vitest": "^4.1.2"
	}
}
